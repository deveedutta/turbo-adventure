import { WalkingCalorieCalculator } from './walking-calorie-calculator';

describe('WalkingCalorieCalculator', () => {
  it('should create an instance', () => {
    expect(new WalkingCalorieCalculator()).toBeTruthy();
  });
});
